# Packages

This folder is reserved for shared packages once the monorepo grows.

Likely future packages:

- shared UI components
- shared TypeScript config
- shared API types
- shared ESLint or tooling config

